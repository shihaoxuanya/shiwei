# 手动发布与回退

当前没有后台发布目标、自动更新或远程撤销能力。GitHub 下载过的文件不能远程收回。

发现发布缺陷时，维护者应在 GitHub Release 明确标记问题、提供更高版本的修复，并说明受影响范围。默认优先前向修复。

任何降级前先备份完整资料库，并在隔离副本验证旧版本的 schema 兼容性。不得修改 schema marker 强迫旧程序打开不认识的新数据库。不能保证旧版本可以读取新版本资料库。
