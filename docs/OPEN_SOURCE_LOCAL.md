# 本地开源调整验收（2026-09-15）

## 范围

- 用户确认 MIT；移除拾微发布后台、统计、错误上报及自动更新。保留自选在线/本地模型、公开网页保存及本地知识功能。
- 新源码不创建统计安装标识，不读取旧统计设置、不补传事件。旧安装包不会随源码更新自行改变。
- 设置保留显式 GitHub 手动下载入口。CI 保留安装包签名与公开产物，不再访问后台。

## 已执行验证

- 前端 134 项通过，Python 420 项通过、1 项跳过，Rust 38 项通过；发布/平台 13 项通过。
- 正式 Tauri signer 合成签名可通过离线校验，篡改文件被拒绝。未重新生成生产私钥。
- TypeScript 和生产前端构建通过；实际构建并启动新的 Tauri 壳及打包 Worker，使用隔离资料库，窗口响应、无黑窗、退出清理通过。此检查是启动检查，不等于完整 UI 逐项验收。
- 前端新增测试确认关于页面挂载不调用后台 IPC，仅点击下载才打开浏览器。
- 服务端停止并禁用 shiwei-control，删除 /opt/shiwei-control、/var/lib/shiwei-control（含内部备份）、服务环境配置及单元。8920 无监听，原域名返回 410。没有改动用户电脑资料库。
- 服务器内后台数据本次没有另做备份。云厂商快照和共享系统日志不在本次删除范围；未声称做过磁盘安全擦除。

## 发布与审计边界

公开前对 Git 历史运行启发式秘密扫描，再扫描最终提交；只输出文件路径与问题类型，不输出匹配内容。启发式扫描不是绝对安全证明。

没有重写历史。历史后台代码和历史验收记录可继续在 Git 中找到；它们不代表当前客户端仍有这些功能。MIT 只覆盖项目可授权的代码，第三方软件与模型保留各自许可。

本轮没有发布新安装包或版本标签，现存 0.3.5 安装包仍是旧产品行为。前端打包体积提示和测试中 React act 提示为已知非阻断项。
